package ro.teamnet.zerotohero.oop.graphicshape;

/**
 * Created by Alexandra.Traila on 7/27/2017.
 */
public class Square extends Shape {
    private int side;

    public Square(int side)
    {
        this.side=side;
    }


    /*11.	Create a new class Square that extends the Shape class with a private int primitive named side and a constructor
    that takes one parameter which initializes the side variable.
    Overwrite the area() method so that it calculates the square area.*/
}
